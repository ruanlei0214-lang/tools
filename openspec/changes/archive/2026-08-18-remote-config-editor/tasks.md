## 1. 配置存取层

- [x] 1.1 新增 `internal/modules/remote/store.go`：定位 `%APPDATA%\embedtools\`（`os.UserConfigDir`），
  给出 `remote-config.json` / `remote-io.json` / `remote-register.json` 三个路径，
  以及一个供界面显示的目录路径。
- [x] 1.2 `store.go` 里实现读：文件不存在返回「没有现场配置」这一状态（不是错误），
  读得出来但内容坏掉时返回错误且带上文件名。
- [x] 1.3 `store.go` 里实现原子写：先写 `.tmp` 再 `os.Rename`，写失败如实返回错误。
- [x] 1.4 `store.go` 里实现删（恢复默认用）：文件不存在算成功，删除失败如实报错。
- [x] 1.5 新增 `store_test.go`：用 `t.TempDir()` 覆盖读不存在、写后能读回、写坏文件报错带文件名、
  删除后退回「没有现场配置」、重复删除不报错。

## 2. 加载与校验

- [x] 2.1 改 `config.go` 的 `loadSettings()`：连接参数与两份点位各自「先读 `%APPDATA%`、
  没有就用内嵌那份」，内嵌三份的注释改成「出厂默认」。
- [x] 2.2 某一份坏掉时只让那一部分退回内嵌默认，告警文字带上文件名与
  `describeParseError` 给出的行列号；多份出问题时把告警拼成一条。
- [x] 2.3 坏文件不覆盖：加载路径只读不写，确认没有任何「顺手修好它」的分支。
- [x] 2.4 抽出保存路径要复用的校验入口（沿用现有 `parseRoot` / `parsePanel` / `normalizeTab`），
  确认加载与保存走的是同一套规则，不新写第二套。
- [x] 2.5 补 `config_test.go`：现场配置覆盖出厂默认、只有一份现场配置时另一份仍用默认、
  某一份坏掉只影响它自己且告警带文件名与行号。

## 3. 后端可重载状态与保存方法

- [x] 3.1 `remote.go` 的 `Service` 加 `cfgMu sync.RWMutex`，`settings` 改为受它保护；
  加一个取快照的私有方法，`connectTimeout()` / `requestTimeout()` 改为走快照。
- [x] 3.2 确认 `cfgMu` 与 `s.mu` 的持有范围不重叠：`client()` 持 `s.mu` 期间不读配置，
  在代码注释里写明这条约束。
- [x] 3.3 加 `SaveDevice`：校验地址、端口、路径、两个超时、刷新间隔的范围，
  通过则落盘并更新内存快照，返回归一化后的值；不通过时不写盘。
- [x] 3.4 加 `SavePanel`：接收整份面板（kind + 分组与点位），跑与加载相同的校验与归一化，
  通过则落盘并更新内存里对应的那个 `Tab`，把归一化后的那份返回给前端。
- [x] 3.5 加 `ResetPanel` / `ResetDevice`：删掉对应的 `%APPDATA%` 文件、重新加载、
  返回退回默认后的值。
- [x] 3.6 确认保存与恢复都不断开已有连接、不向控制器发任何请求。
- [x] 3.7 补 `service_test.go`：保存点位后 `Config()` 返回新清单、保存越界值被拒且盘上不变、
  保存不影响连接状态、恢复默认后退回出厂值。

## 4. 前端：点位编辑

- [x] 4.1 新增 `frontend/src/modules/remote/PointEditor.vue`：一行内联表单，字段为名称、类型、
  端口/地址、`onValue`、`offValue`、`pulseMs`、`danger`、`hint`；类型可选值与端口标签由 prop 传入。
- [x] 4.2 `IoPanel.vue` 接上 `PointEditor`：分组内每个点位加编辑/删除图标，组尾加「添加点位」，
  卡片头加「添加分组」「改组名」「删除分组」。
- [x] 4.3 `RegisterPanel.vue` 接上同一个 `PointEditor`，只传不同的类型可选值与端口标签，
  其余布局与文字保持一致。
- [x] 4.4 删除点位与删除分组弹确认框，分组的确认框里说明这一组有多少个点位。
- [x] 4.5 保存调 `SavePanel`，成功后用后端返回的那份替换本地清单并让当前页重画；
  失败时把后端的原因显示在状态栏，本地清单不动。
- [x] 4.6 新增/改过端口的点位在被刷新到之前显示 `—`，不显示 `OFF`。
- [x] 4.7 编辑区显示配置文件所在位置（挂 `title` 说明拷走 exe 不会带走它）。

## 5. 前端：连接参数与立即生效

- [x] 5.1 `RemoteView.vue` 的连接区补上路径、建连超时、请求超时、刷新间隔，
  和「保存」「恢复默认」；按设计里的取舍决定是常显还是展开显示，保证连接区不被撑成两行以上。
- [x] 5.2 保存连接参数调 `SaveDevice`，越界时把原因显示在顶部状态槽，不写盘。
- [x] 5.3 保存后地址与当前连接不一致时，提示需要点「连接」才生效，且不自动断开、不自动重连。
- [x] 5.4 `IoPanel` / `RegisterPanel` 把定时器改成 `watch(() => props.intervalMs)` 重建，
  删掉「配置只在启动时读一次」那句已经不成立的注释。
- [x] 5.5 确认「每 N 秒自动刷新」的文字跟着新间隔变。

## 6. 文档与收尾

- [x] 6.1 `doc/remote.md`：配置文件一节改成「出厂默认 vs 现场配置」，写清存放位置、
  立即生效的范围、地址类改动要重连、恢复默认、坏文件的兜底。
- [x] 6.2 `doc/remote.md`：界面操作一节补点位编辑与连接参数编辑的步骤；
  后端接口表加上新增的四个方法；相关文件清单加上 `store.go` 与 `PointEditor.vue`。
- [x] 6.3 `doc/remote.md` 的已知限制补两条：界面保存会整份覆盖盘上那份、拷走 exe 不带配置。
- [x] 6.4 `frontend/src/modules/remote/module.ts` 版本号跳一位，`doc/README.md` 的表格跟着改。
- [x] 6.5 根 `README.md` 的「远程控制」一段改成「配置在界面上改、改完立即生效」。
- [ ] 6.6 跑 `go test ./...`，然后 `wails build`，手工过一遍：加点位、改端口、越界被拒、
  改刷新间隔、恢复默认、手动改坏 `%APPDATA%` 那份后重开看告警。
