## 1. 合并连接与读取

- [x] 1.1 `NetcfgView.vue`：把 `refresh()` 的函数体并进 `test()`——`TestConnection` 之后接 `ListInterfaces`，空列表时提示「连接成功，但设备上没有找到可配置的网口。」，否则 `select()` 第一个 UP 的网口再写「连接成功」。
- [x] 1.2 删掉 `refresh()` 函数，确认没有残留调用点（`onMounted` 调的是 `test()`，不受影响）。
- [x] 1.3 确认成功提示写在 `ListInterfaces` 之后，使读网口的失败能覆盖「连接成功」。
- [x] 1.4 `test()` 开头清空 `ifaces`、`selected`、`confirming`，让连接失败后界面不残留上一次的网口数据。

## 2. 界面调整

- [x] 2.1 模板里删掉「读取网口」按钮，同时移除只服务于它的 `busy === 'refresh'` 判断。
- [x] 2.2 「测试连接」按钮加 `class="primary"`。

## 3. 文案订正

- [x] 3.1 `apply()` 成功提示里的「请重新读取网口确认」改为「请重新点『测试连接』确认」。
- [x] 3.2 `doc/netcfg.md` 界面操作第 2 条改为说明连接成功会顺带读出网口，第 4 条的「读取网口」改为「测试连接」。
- [x] 3.3 `doc/netcfg.md`「上次用过的地址」里列举的记录时机去掉已不存在的「读取网口」按钮。

## 4. 验证

- [x] 4.1 前端无 lint 报错。
- [x] 4.2 `.\build.ps1` 构建通过。
- [ ] 4.3 人工确认：打开页面自动连一次后表格已填充且第一行选中；改地址下发后提示指向「测试连接」；设备连不上时只报连接失败。
- [ ] 4.4 人工确认清空行为：先连通一台设备读出网口，再把地址改成一个连不上的，点「测试连接」，等超时后表格与修改表单都应消失，只剩失败提示。
